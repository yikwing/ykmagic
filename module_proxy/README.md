# 十六进状态机

```
/**
 * 当我们需要往状态集中添加状态时，就通过或运算。例如：
 * STATUSES | STATUS_1
 * */


/**
 * 当我们需要从状态集中移除状态时，就通过取反运算。例如：
 * STATUSES & ~ STATUS_1
 * */


/**
 * 当我们需要判断状态集中是否包含某状态时，就通过与运算。结果为 0 即代表无，反之有。
 * (statuses & status) != 0
 * */
```
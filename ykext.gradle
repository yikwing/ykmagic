import groovy.json.JsonSlurper

def jsonEnvFile = rootProject.file("./android_env.json")

def InjectJson = new JsonSlurper().parseText(jsonEnvFile.text)

ext {
    injectEnv = InjectJson

    // App dependencies
    core_ktx_version = "1.9.0-alpha03"
    appcompat_version = '1.6.0-alpha03'
    constraintlayout_version = "2.1.3"
    lifecycle_runtime_ktx_version = '2.5.0-beta01'
    recyclerview_version = "1.3.0-alpha02"
    fragment_ktx_version = "1.5.0-beta01"

    material_version = "1.7.0-alpha01"

    room_version = '2.5.0-alpha01'

    moshi_version = "1.13.0"

    coroutines_version = "1.6.2"


    okhttp_version = "5.0.0-alpha.7"

    retrofit_version = "2.9.0"


    coil_version = "2.1.0"
    glance_version = "1.1.0"

    junit_version = "4.13.2"
}

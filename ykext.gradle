import groovy.json.JsonSlurper

def jsonEnvFile = rootProject.file("./android_env.json")

def InjectJson = new JsonSlurper().parseText(jsonEnvFile.text)

ext {
    injectEnv = InjectJson

    // App dependencies
    coreKtxVersion = "1.9.0-alpha03"
    appCompatVersion = '1.6.0-alpha03'
    constraintlayoutVersion = "2.1.3"
    lifecycleRuntimeKtxVersion = '2.5.0-beta01'
    fragmentKtxVersion = "1.5.0-beta01"

    materialVersiob = "1.7.0-alpha01"

    roomVersion = '2.5.0-alpha01'

    moshiVersion = "1.13.0"

    coroutinesVersion = "1.6.1"


    okhttpVersion = "5.0.0-alpha.7"

    retrofitVersion = "2.9.0"


    coilVersion = "2.1.0"
    glanceVersion = "1.1.0"

    junit = "4.13.2"
}
